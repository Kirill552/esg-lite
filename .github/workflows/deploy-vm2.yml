# Автоматический деплой ESG-Lite на VM2
name: Deploy to VM2

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  deploy:
    name: Deploy to VM2
    runs-on: ubuntu-latest
    timeout-minutes: 30  # Увеличиваем до 30 минут
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Deploy to VM2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VM2_HOST }}
        username: ${{ secrets.VM2_USER }}
        key: ${{ secrets.VM2_SSH_KEY }}
        port: 22
        timeout: 25m  # Увеличиваем SSH таймаут
        command_timeout: 20m  # Увеличиваем таймаут команд
        script: |
          echo "🚀 Начинаем деплой ESG-Lite..."
          
          # Переходим в директорию проекта
          cd /opt/esg-lite || { echo "❌ Директория /opt/esg-lite не найдена"; exit 1; }
          
          # Создаем локальные SSL сертификаты если их нет
          if [ ! -f ./ssl/nginx-selfsigned.crt ]; then
            echo "🔐 Генерируем SSL сертификат..."
            mkdir -p ./ssl
            openssl genrsa -out ./ssl/nginx-selfsigned.key 2048
            openssl req -new -x509 -key ./ssl/nginx-selfsigned.key \
              -out ./ssl/nginx-selfsigned.crt -days 365 \
              -subj "/C=RU/ST=Moscow/L=Moscow/O=ESG-Lite/CN=82.202.156.35"
            chmod 600 ./ssl/nginx-selfsigned.key
            chmod 644 ./ssl/nginx-selfsigned.crt
          fi
          
          # Останавливаем контейнеры
          echo "⏹️ Останавливаем контейнеры..."
          docker compose -f docker-compose.prod.yml down
          
          # Обновляем код
          echo "📥 Обновляем код из GitHub..."
          git fetch origin
          git reset --hard origin/main
          
          # Проверяем что authorized_key.json существует
          if [ ! -f authorized_key.json ]; then
            echo "⚠️ authorized_key.json не найден, но это нормально для безопасности"
          fi
          
          # Удаляем старые образы
          echo "🧹 Очищаем старые образы..."
          docker image prune -f
          docker system prune -f
          
          # Принудительно пересобираем образы (поэтапно для экономии времени)
          echo "🔥 Сначала собираем веб и nginx контейнеры..."
          docker compose -f docker-compose.prod.yml build --no-cache --pull nginx esg-lite-web metrics-exporter
          
          echo "🔥 Теперь собираем worker контейнер отдельно (может занять до 10 минут)..."
          timeout 900 docker compose -f docker-compose.prod.yml build --no-cache --pull esg-lite-worker || {
            echo "⚠️ Worker билд превысил 15 минут, пропускаем и используем существующий образ"
            docker compose -f docker-compose.prod.yml build esg-lite-worker || echo "Используем кешированный образ"
          }
          
          # Собираем и запускаем новые контейнеры
          echo "🔨 Запускаем контейнеры..."
          docker compose -f docker-compose.prod.yml up -d
          
          # Проверяем успешность запуска
          echo "⏳ Ждем запуска сервисов..."
          sleep 30
          
          # Диагностика после запуска
          echo "🔍 Диагностика контейнеров после запуска..."
          docker compose -f docker-compose.prod.yml ps
          
          # Проверяем логи если есть проблемы
          echo "📋 Логи контейнеров:"
          docker compose -f docker-compose.prod.yml logs --tail=30
          
          # Применяем миграции только если веб-контейнер запущен
          if docker compose -f docker-compose.prod.yml ps esg-lite-web | grep -q "Up"; then
            echo "🗃️ Применяем миграции базы данных..."
            docker compose -f docker-compose.prod.yml exec -T esg-lite-web sh -c 'DATABASE_URL="${{ secrets.PRODUCTION_DATABASE_URL }}" npx prisma migrate deploy' || echo "⚠️ Ошибка применения миграций"
            
            echo "📊 Проверяем актуальность коэффициентов выбросов..."
            docker compose -f docker-compose.prod.yml exec -T esg-lite-web sh -c 'node scripts/update-emission-factors.js --check || echo "⚠️ Коэффициенты требуют обновления после деплоя"'
          else
            echo "❌ Веб-контейнер не запущен, пропускаем миграции"
          fi
          
          # Дополнительное ожидание
          echo "⏳ Дополнительное ожидание запуска..."
          sleep 30
          
          # Проверяем статус
          echo "✅ Проверяем статус контейнеров..."
          docker compose -f docker-compose.prod.yml ps
          
          # Проверяем логи
          echo "📋 Последние логи:"
          docker compose -f docker-compose.prod.yml logs --tail=20
          
          echo "🎉 Деплой завершен!"

    - name: Health Check
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VM2_HOST }}
        username: ${{ secrets.VM2_USER }}
        key: ${{ secrets.VM2_SSH_KEY }}
        port: 22
        script: |
          cd /opt/esg-lite
          echo "🏥 Проверяем здоровье приложения..."
          
          # Дополнительное ожидание
          echo "⏳ Дополнительное ожидание запуска контейнеров..."
          sleep 30
          
          # Диагностика контейнеров
          echo "🔍 Проверяем статус контейнеров..."
          docker compose -f docker-compose.prod.yml ps
          
          # Проверяем логи если контейнеры не работают
          echo "📋 Всегда показываем логи для диагностики:"
          docker compose -f docker-compose.prod.yml logs --tail=100 || echo "Нет логов"
          
          if ! docker compose -f docker-compose.prod.yml ps | grep -q "Up"; then
            echo "❌ Контейнеры не запущены! Дополнительная диагностика:"
            
            echo "🔍 Проверяем образы:"
            docker images | grep esg-lite || echo "Образы esg-lite не найдены"
            
            echo "🔍 Проверяем состояние Docker:"
            docker info | head -10
            
            echo "🔍 Проверяем файлы в проекте:"
            ls -la
            
            echo "🔍 Проверяем Dockerfile:"
            head -20 Dockerfile || echo "Dockerfile не найден"
            
            exit 1
          fi
          
          echo "🔍 Проверяем какие порты слушаются..."
          ss -tlnp | grep -E ":80|:443|:3000" || echo "Не все порты слушаются"
          
          # Проверяем доступность приложения через nginx
          echo "🌐 Проверяем доступность through nginx..."
          if curl -f -k -m 10 https://localhost/api/queue/health; then
            echo "✅ Приложение работает корректно через HTTPS!"
          elif curl -f -m 10 http://localhost/api/queue/health; then
            echo "✅ Приложение работает через HTTP!"
          else
            echo "❌ Приложение недоступно через nginx, проверяем напрямую контейнер..."
            
            # Проверяем веб-контейнер напрямую
            if docker compose -f docker-compose.prod.yml exec -T esg-lite-web curl -f -m 5 http://localhost:3000/api/queue/health; then
              echo "⚠️ Веб-контейнер работает, но nginx не проксирует"
              echo "🔍 Проверяем конфигурацию nginx..."
              docker compose -f docker-compose.prod.yml logs nginx --tail=20
            else
              echo "❌ Веб-контейнер не отвечает"
              echo "📋 Логи веб-контейнера:"
              docker compose -f docker-compose.prod.yml logs esg-lite-web --tail=50
            fi
            
            echo "📋 Логи всех контейнеров:"
            docker compose -f docker-compose.prod.yml logs --tail=30
            exit 1
          fi
          
          echo "🎉 Проверка здоровья завершена успешно!"
          echo "🌐 Сайт доступен по адресу: https://esg-lite.ru"

    - name: Notify Success
      if: success()
      run: |
        echo "🎉 Деплой на VM2 успешно завершен!"
        echo "🌐 Приложение доступно по адресу: https://esg-lite.ru"

    - name: Notify Failure
      if: failure()
      run: |
        echo "❌ Деплой на VM2 завершился с ошибкой!"
        echo "🔍 Проверьте логи выше для диагностики проблемы"